apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  namespace: auth
  name: forward-auth
spec:
  forwardAuth:
    address: http://gateway.auth:80/verify
    trustForwardHeader: true
    authResponseHeaders:
      - X-Auth-User-Id
      - X-Auth-Email
      - X-Auth-Roles
