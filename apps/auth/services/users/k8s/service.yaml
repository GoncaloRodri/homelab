---
apiVersion: v1
kind: Service
metadata:
  namespace: auth
  name: users
  labels:
    app: users
spec:
  type: ClusterIP
  selector:
    app: users
  ports:
    - name: http
      port: 80
      targetPort: http
